exports.encrypt = (k, t) => encrypt(k, t);

function encrypt(key, text)
{

}

exports.brute = (c, k) => brute(c, k);
// BRUTE FORCE
function brute(cipher, known_text)
{
  for (int i = 0; i <= cipher.length; i++)
}
